# This file is used for git to track and keep the folder if it is empty.
# The folder is one of the starting locations where Ansible start
# its search from when relative path is used in "ansible.builtin.include_vars".
