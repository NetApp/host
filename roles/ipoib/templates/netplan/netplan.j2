# Managed by NetApp E-Series Ansible

network:
  version: 2
  renderer: networkd
  ethernets:
    {{ item["name"] }}:
      addresses:
        - {{ item["address"] }}
      mtu: {{ item["mtu"] | default(eseries_ipoib_mtu) }}
