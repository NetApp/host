# Managed by NetApp E-Series Ansible

network:
  version: 2
  renderer: networkd
  ethernets:
    {{ interface["name"] }}:
      addresses:
        - {{ interface["address"] }}
      mtu: {{ interface["mtu"] | default(eseries_ipoib_mtu) }}
